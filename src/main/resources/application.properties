spring.application.name=authService
# Database connection properties
server.port = 8097
# Database connection properties
# PostgreSQL DataSource
spring.datasource.url=jdbc:postgresql://localhost:5432/userdb
spring.datasource.username=prasannakrishnabhagwat
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/userdb
spring.data.mongodb.auto-index-creation=true
spring.security.user.name=admin
spring.security.user.password=admin123
# Optional: Specify Log4j2 configuration file path
#logging.config=classpath:log4j2.xml
logging.level.org.springframework.security=DEBUG

# Keycloak
keycloak.server-url=http://localhost:8181
keycloak.realm=scm
keycloak.client-id=scm-backend
keycloak.client-secret=JWNcEsWwB55seZR4nYd4feW0AD7o1A7g

# JWT validation via Keycloak JWKS
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8181/realms/scm

# UserService base URL for fetching user details post-authentication
userservice.base-url=http://localhost:8087

logging.config=classpath:logback-observability.xml